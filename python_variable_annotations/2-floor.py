#!/usr/bin/env python3
"""Module that provides a type-annotated floor function."""
import math


def floor(n: float) -> int:
    """Returns the floor of a given float as an integer."""
    return math.floor(n)
